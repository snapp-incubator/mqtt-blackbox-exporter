---
version: "2"
linters:
  default: all
  disable:
    - depguard
    # we don't use json with camel-case
    - tagliatelle
    - nolintlint
    # it should improve to support more known patterns
    - varnamelen
    - ireturn
    - revive
    - wrapcheck
    - gosec
    # superseded by wsl_v5 and gomodguard_v2, both already on via `default: all`
    - wsl
    - gomodguard
